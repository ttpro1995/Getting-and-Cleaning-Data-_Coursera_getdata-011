housing_data<-read.csv("getdata-data-ss06hid.csv")
worth<-subset(housing_data,VAL==24)
# we see that worth have 53 obs. (In global Environment window, top right)
#How many properties are worth $1,000,000 or more? answer 53
